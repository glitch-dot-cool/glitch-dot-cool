const blogPostsQuery = require("./blogPosts")
const profilesQuery = require("./profiles")
const tagsQuery = require("./tags")
const projectsQuery = require("./projects")

module.exports = { blogPostsQuery, profilesQuery, tagsQuery, projectsQuery }
