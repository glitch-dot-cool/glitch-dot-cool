const blogPostsQuery = require("./blogPosts")
const profilesQuery = require("./profiles")
const tagsQuery = require("./tags")
const projectsQuery = require("./projects")
const releasesQuery = require("./releases")
const communityPostsQuery = require("./communityPosts")

module.exports = {
  blogPostsQuery,
  profilesQuery,
  tagsQuery,
  projectsQuery,
  releasesQuery,
  communityPostsQuery,
}
